// Placeholder rekonstruisan 2026-05-13 zbog OneDrive dehidracije (D.1).
// Pun runbook: docs/OMNIGROUP-WEB-EMPTY-FILES-RUNBOOK.md.
// Minimalan App Router segment layout — passthrough <>{children}</>.
// TODO[D.1-restore]: ako je originalni dev/layout imao auth gate / wrap, vratiti.

export default function DevLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
