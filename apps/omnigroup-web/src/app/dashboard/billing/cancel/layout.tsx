import BillingResultShell from '../BillingResultShell';

export default function BillingCancelLayout({ children }: { children: React.ReactNode }) {
  return <BillingResultShell>{children}</BillingResultShell>;
}
