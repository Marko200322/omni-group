node_modules
.next
node_modules/.cache/omnigroup-next
.git
*.log
# Exclude local/secret envs from build context; allow production public+runtime env
# so Next can bake NEXT_PUBLIC_* (phase, budget, API base) at `npm run build`.
.env*
!.env.production
!.env.production.example
coverage
.turbo
