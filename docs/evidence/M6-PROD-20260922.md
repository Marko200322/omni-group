# M6 prod rollout — 2026-09-22

| Item | Status |
|------|--------|
| `deploy.config.json` | `factoryPhase=M6`, `prodMode=full` |
| VPS deploy | SafeDeploy OK |
| `FACTORY_PHASE` on API | **M6**, `ready=true` |
| Catalog | **50 × 20** matrix verify PASS |
| Stripe env | `PAYMENTS_MODE=live` (keys in deploy.config) |
| Lead DB | **F5**, autonomy **ON** (M6 profile) |
| Finish repo scale | `run-vps-finish-repo.ps1 -UseProdDeployPath` → `/var/log/omni-finish-repo-test.log` |

**Scripts:** `.\scripts\m6-prod-rollout.ps1` · `.\scripts\verify-factory-phase.ps1 -FactoryPhase M6`

**Still owner/ops (checklist, not deploy blockers):** Stripe webhook live proof, HeyGen/D-ID if premium avatar required, `OUTREACH_SEND_ENABLED` for mass cold send, LLC fields on invoices.
