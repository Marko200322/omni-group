# Env: SITE_DOMAIN (web + API paths on one TLS cert)

{$SITE_DOMAIN} {
	# Public API (works without separate api.* DNS)
	handle /api/v1/* {
		reverse_proxy atina-api:3000
	}
	handle /health {
		reverse_proxy atina-api:3000
	}
	reverse_proxy web:3000
}
