apiVersion: apps/v1
kind: Deployment
metadata:
  name: omnigroup-web
  labels:
    app: omnigroup-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: omnigroup-web
  template:
    metadata:
      labels:
        app: omnigroup-web
    spec:
      containers:
        - name: omnigroup-web
          image: ghcr.io/omni-group/omnigroup-web:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              name: http
          envFrom:
            - secretRef:
                name: omnigroup-web-env
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3000"
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
