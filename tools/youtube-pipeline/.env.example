# YouTube pipeline — kopiraj u .env ILI koristi atina-platform/atina/.env (auto-load)
# AI/SCRAPER/YOUTUBE_* se čitaju iz Atina .env kad servis startuje iz monorepa.

PIPELINE_HTTP_PORT=8090
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
LOCAL_STORAGE_PATH=./data
OUTPUT_PATH=./data/output
MAX_WORKERS=4
